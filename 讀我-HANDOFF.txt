============================================================
3D 中國象棋(3d-chinese-chess)— 交接說明(讀我-HANDOFF.txt)
更新:2026-09-10(agape250 機・Sonnet 5)🐛 修好黑方沒反應/棋盤憑空多子(v14,三站已部署:指紋 index-Urjh1ODy.js)
============================================================

★★★★★ 2026-09-10(agape250 機・Sonnet 5)🐛 修好嚴重 bug:黑方偶爾完全沒反應 + 棋盤憑空多子 ✅ 已上線(e67e360,v14)★★★★★
  由來:使用者三張實機截圖回報退件①「炮走一步後黑方完全沒反應」②「炮跟砲之間沒有棋子,
       為何砲會變紅色?BUG」③「我走右邊炮後,黑方一次走兩步」。
  用真滑鼠點擊(Playwright `page.mouse.click`,不是 `page.evaluate` 繞過事件系統)重演,
  第一次就在 console 抓到未 minify 的堆疊:
    PAGEERROR: Cannot access 'minimax' before initialization
      at getBestMoveAlphaBeta (src/game/ai.js) ← at makeAIMove (src/App.jsx)
  ★★ 真因:`getBestMoveAlphaBeta` 裡 `const minimax = ...` 宣告在函式後段(原第 472 行),
    但「黑方要不要照開局書走」那段安全檢查(原第 463 行)早就在用它——JS `const`/`let`
    有暫存死區(TDZ),同一作用域內、宣告那一行**執行之前**引用會直接丟 ReferenceError。
    openingStyle 預設就是 'auto',黑方開局前幾手幾乎每一局都會踩到,不是偶發。
    連鎖後果更嚴重:那段先 `engine.move(...)` 把測試手真的套用到共用 engine 物件上,
    再呼叫 `minimax(...)` 驗證安不安全,驗完 `engine.undo()` 撤銷——TDZ 例外剛好炸在
    move 與 undo **之間**,undo 永遠不會執行,一步「洩漏」的測試手就永久黏在真正盤面上。
    例外一路炸出 makeAIMove,React 的 `boardState` 沒機會同步 ⇒ 畫面完全沒變化(①);
    下一次任何操作觸發重繪就冒出這顆憑空多出來的棋子(③);如果它剛好卡進兩尊炮中間,
    炮就會被正確判定「吃得到」而變紅,但玩家螢幕還停在舊畫面,才會覺得「中間明明沒棋子」(②)。
    **三件退件,一個真因。**
  改動(src/game/ai.js):
    ① `const minimax = ...` 搬到函式最前面,第一次被用到之前就先宣告好。
    ② 「先試走、算完再收回去」那段加 try/finally:不管 minimax 順不順利,undo 一定執行,
       以後這附近再出別的例外也不會再洩漏測試手到真正盤面上。
  守門(引擎層 + 端對端瀏覽器層都補了永久測試):
    · test/ai.mjs 第④節:紅方先動一手(讓黑方真的輪到用開局書),對每一種 openingStyle
      斷言「不拋例外」+「給得出合法一手」+「engine.history 長度沒有洩漏殘留」。
      ★ 突變驗過:宣告搬回原位重現 TDZ ⇒ 15/15 條當場紅(auto/cannon/screen_horse/
      elephant/pawn 五種書都炸,只有 'none' 沒事——精準對上「幾乎每局都中」)。
    · scripts/check-mobile-ui.mjs 第⑥節:真滑鼠點擊選紅炮、走一步、等 AI,斷言零例外、
      turn 真的輪回紅方、棋子總數沒有憑空增減。引擎層測試證明不了「畫面上點了會不會動」,
      這是使用者實際互動發現的 bug,所以補真點擊版本。
    · `window.__anchess` 加 `screenPosFor(x,y)`:借用真相機的投影矩陣算格座標的真實螢幕
      像素,讓自動化測試能用 `page.mouse.click` 走真正的 raycast onClick。
  測試:npm test(ai 29・version 62・legality 20・fit 24)、check:mobile 19/0,全綠。
  部署:npm run deploy(第一站成功後 wrangler 收尾又是那個已知的本機原生崩潰,手動補推
    另外兩站);三站指紋一致 index-Urjh1ODy.js,線上驗過(含 curl 下載 bundle 確認
    `screenPosFor`/`v14` 字樣都在線上)。
  git 實況(交接前真的問過 git):status --short 空、HEAD e67e360、rev-list @{u}..HEAD = 0。

★★★★★ 2026-09-10(agape250 機・Opus 5)📐 橫式棋盤加寬到超過姊妹站 + 俯角 62° ✅ 已上線(dd03c98,v13)★★★★★
  由來:使用者「橫式棋盤要更寬,至少跟 3d-xiangqi.pages.dev 與 incandescent-stroopwafel-31007a
       一樣寬」。這是 v9/v10 兩輪都沒解掉、當時判定「要換鏡頭邏輯」的那條。
  ★★ 真因(前兩輪一直在調鏡頭,方向就錯了):**是棋盤本身的比例不一樣**。
     姊妹站兩站的 renderer 寫著 `SQUARE_SIZE_X = 10; SQUARE_SIZE_Y = 8.5;`
     (註解:「讓棋盤長度(Y軸)短一點,符合視覺比例」)⇒ 棋盤 90×85,比例 1.06;
     本站是 9×10 的**正方格**,比例 0.9。橫式螢幕的 fit 一定被「深度」卡住
     (寬度那邊還有一大堆餘裕),深度短一點鏡頭才靠得近、寬度才填得滿。18% 的比例差
     就是三站並排「這站比較小」的全部原因 —— 不是相機參數、不是 margin。
  改動:
    ① 新增 `src/boardLayout.js` 當**單一真相來源**(RANK_GAP=0.85、gridToWorld /
       worldToGrid、棋子三段半徑)。格線 / 棋子 / 提示標記 / 點擊反算 / 相機 fit
       五處全部改用它——以前各檔各自寫死 `y - 4.5`,漏一處就「棋子跟格線對不齊」且不報錯。
    ② 棋子等比縮到最寬 0.400(RANK_GAP 只有 0.85,不縮上下會疊);這組數字正是姊妹站
       PIECE_RADIUS=4 那組除以 10。
    ③ 俯角 75° → **62°**。兩個要求方向相反(越陡→後排字越清楚,但棋盤越窄),
       用投影數學把兩個指標一起算(844×390,姊妹站基準 底排 46.8% / 後排格距 34.2px):
         75° → 43.3% / 39.1px(不夠寬) 65° → 46.1% / 37.1px(差一點)
         62° → **47.0% / 36.6px(寬度達標,後排還比姊妹站大 7%)**
       ⇒ 62° 是還能達到寬度要求的最陡角度。
    ④ 🖱 順手修掉既有 bug:handleBoardClick 寫死 `/1.2`(桌機 group scale),手機是 1.0
       ⇒ 點空格會算到隔壁格。
  守門:test/fit.mjs 第⑥節把**使用者的驗收標準寫成測試**(本站底排跨距 ≥ 姊妹站),
    ★ 突變驗過(角度改回 75° 當場紅);第⑤節俯角斷言與 check-mobile-ui 第③節同步改 62°。
  ⚠⚠ 量測教訓:一開始用「顏色偵測棋盤像素」跨站比對,量到姊妹站 53.8%(實際 46.8%)
    —— 兩站棋盤顏色不同、版本徽章是亮色會被誤判,害我追錯方向一輪。
    **跨站比大小不要用顏色偵測截圖,用同一套投影數學算。**
  測試:npm test(ai 11・version 58・legality 20・fit 24)、check:mobile 15/0,全綠。
  部署:三站指紋一致 index-D0t4HvsK.js,線上驗過。
  git 實況(交接前真的問過 git):status --short 空、HEAD dd03c98、rev-list @{u}..HEAD = 0。

★★★★★ 2026-09-10(agape250 機・Opus 5)🎥 俯角 75° 真的生效 + 🖐 旋轉降靈敏 ✅ 已上線(19ca426,v11)★★★★★
  ★★ 本輪最重要的一條(**上一版的交接文件說了謊,在這裡更正**):
     v10 寫「俯角改 75 度」——**實際上沒有生效**。App.jsx 的
     `<OrbitControls minPolarAngle={Math.PI/6}>`(30° 極角 = 俯角上限 60°)把 fitCamera
     設的 75° 夾成 **60.0°**;使用者看到的一直是 60°,所以他才接著說「再加 7 度」。
     而 test/fit.mjs 第⑤節只斷言「DIR_3D 這個常數是 75」⇒ 全綠。
     **那是在測「我寫了什麼」,不是「畫出來什麼」。**
     ⇒ 同一族的坑(設了不代表生效)這個 repo 一天內出現兩次(另一次是 fitRef 閉包過期)。
       改任何「相機/控制器」參數,一律用真瀏覽器量結果,不要只斷言常數。
  改動:
    ① `src/fitCamera.js` 抽出 `DIR_3D_ELEVATION_DEG = 75`(使用者中途說過 82,最後拍板 75),
       檔頭寫明「改角度一定要同步檢查 App.jsx 的 minPolarAngle 容不容得下」。
    ② `src/App.jsx` 的 `minPolarAngle`:`Math.PI/6` → `Math.PI/36`(5° 極角 = 俯角上限 85°)。
       不設 0 是為了離正上方的萬向鎖遠一點,同時留手動再轉陡的空間。
    ③ 🖐 `rotateSpeed={IS_COARSE_POINTER ? 0.4 : 1.0}`(使用者:「棋盤旋轉太快太靈敏」)。
       旋轉量 = 2π × 拖曳像素 ÷ 容器高 × rotateSpeed;預設 1.0 在手機劃 150px 轉掉約 64°。
       0.4 與姊妹站 3D-Xiangqi / xiangqi-arena 同值;滑鼠維持 1.0。
  守門(這次補的是真瀏覽器量出來的**結果**):
    · check-mobile-ui 第③節:量 `window.__anchess.camElevation` == 75°±1.5。
      ★ 突變驗過:minPolarAngle 改回 `Math.PI/6` ⇒ 當場量到 60.0° 變紅。
    · 第④節:另開 hasTouch context 量 `rotateSpeed`(觸控 0.4 / 滑鼠 1.0)——
      `pointer: coarse` 是裝置能力,`setViewportSize` 改不出來,一定要換 context。
    · `window.__anchess` 加唯讀 `camElevation` / `rotateSpeed`。
  測試:npm test(ai 11・version 50・legality 20・fit 23)、check:mobile 15/0,全綠。
  部署:npm run deploy(第一站成功後 wrangler 收尾又是那個已知的本機原生崩潰,手動補推
    另外兩站);三站指紋一致 index-D_Z0OXZj.js,線上驗過。
  📋 仍未解、使用者已知情:「橫式棋盤要更寬/棋子間距」——見 v10 那段的查證結論
    (要換一套鏡頭邏輯才做得到,不是調參數能安全達成的);使用者說「下次評估要多少改動」。
  git 實況(交接前真的問過 git):status --short 空、HEAD 19ca426、rev-list @{u}..HEAD = 0。

★★★★★ 2026-09-10(agape250 機・Sonnet 5)🎥 俯角改 75 度,後排文字更清楚 ✅ 已上線(b28f23a,v10)★★★★★
  由來:使用者這輪一次提五件:①「重置視角還需要朝上順時鐘再轉30度」②「後排黑色棋子的字
       太小,看不清楚」③「橫式棋盤仍沒有其他象棋大」④「棋子與棋子之間的距離較窄」
       ⑤「棋盤寬度可以更寬」。
  ① + ②(有做,一個改動解兩個):`DIR_3D` 俯角 45°→75°(照使用者指定的角度)。文字是
    平貼在棋子頂面的,斜看時離鏡頭越遠那排字被壓得越扁——這就是後排字看不清楚的病根。
    角度越接近正上方,近/遠排字的清晰度差距越小。附帶好處:直向棋盤在新角度下反而變大了
    一點(390×844 distance 23.5→21.4,精算法本來就有這個特性)。
  角度變陡之後,橫式那支簡化公式(v9 加的)的 margin 從 1.04 補到 1.10(量過 75° 時
    844×390 的 8 角精算最小安全距離漲了約 5%,舊 margin 不夠了),多種橫式尺寸重新驗證過。
  ③④⑤(誠實查證後**這輪沒有安全解法**,已寫進 v10 改版簡歷讓使用者知道):棋盤 9 寬×10
    深的固定比例,跟超寬手機橫式螢幕比例差很多;現在「鏡頭盯著中心點、長寬都要不裁切」的
    算法,數學上一定被比較「窄」的維度卡住(通常是高度),寬度雖然有餘裕,硬拉近去填滿寬度
    一定會切到最前/最後排棋子——跟上一輪 margin 調到 0.74 裁到俥是同一個坑,這次事先用
    矩陣驗算擋掉了。要真的做到跟姊妹站一樣寬,得換一種鏡頭邏輯(非對稱視錐或不同的留白
    規則),超出這輪安全範圍,沒有動。
  新增 test/fit.mjs 第⑤節:鎖住 DIR_3D 俯角=75°,以後有人改動會被測試抓到。
  測試:npm test(ai 11・version 46・legality 20・fit 23)、check:mobile 11/0,全綠。
  部署:npm run deploy(第一站成功,wrangler 收尾又是那個已知的本機崩潰,手動補推另外
    兩站);三站指紋一致 index-C8zO7evJ.js,線上驗過 v10 文案在。
  git 實況(交接前真的問過 git):status --short 空、HEAD b28f23a、rev-list @{u}..HEAD = 0。

★★★★★ 2026-09-10(agape250 機・Sonnet 5)📐 手機橫式棋盤變大 ✅ 已上線(f68591b,v9)★★★★★
  由來:使用者拿「3D 象棋」「3D 象棋對局場」兩站的橫式截圖跟本站一比,發現本站明顯比較小,
       要求「請讓橫式棋盤變大」。
  查證(教訓寫下來,下次遇到類似情況別重踩):
    · 同一視窗(844×390)量出來本站相機距離 15.56,姊妹站那款「棋盤攤平配 fov」簡化公式
      只要 12.07,差 29%,肉眼看得出。
    · ⚠⚠ **不能直接調 margin 了事**:試過 0.86/0.74,橫式截圖看起來變大、也「看起來」
      沒裁切,但真的跑 test/fit.mjs 矩陣投影驗證才抓到 0.74 那組在**直向**會裁到邊路棋子
      (俥被切一半)——正是 0909 修過的舊病,自己重踩一腳。目測截圖抓不到裁切,一定要
      矩陣投影驗證。
    · 真病根:本站的 8 角精算法比姊妹站的簡化公式嚴謹(保證連最貼近鏡頭那顆棋子的頂面
      邊角都不被切),這份嚴謹在直向極窄畫面是救命符(0909),但橫式寬螢幕本來就有餘裕,
      精算法在那裡保守到肉眼看得出差異。
  改動(src/fitCamera.js):只在「3D + 橫式(aspect≥1)」改用簡化公式,直向/2D 模式完全
    不動。多種手機/平板/桌機橫向尺寸都測過,worst 穩定在 99.1%(在視錐內、沒裁切)。
  測試:test/fit.mjs 新增第④節(橫式真的更貼近 + 依然不裁切 + 不是換公式沒生效),
    ★ 突變驗過會咬。npm test(ai 11・version 42・legality 20・fit 22)、check:mobile 11/0,全綠。
  部署:npm run deploy(第一站成功後 wrangler 收尾又是那個已知的本機原生崩潰,手動補推
    3dchinesechess / 3dchinese);三站指紋一致 index-TzqJfhRi.js,線上驗過 v9 文案在。
  git 實況(交接前真的問過 git):status --short 空、HEAD f68591b、rev-list @{u}..HEAD = 0。

★★★★★ 2026-09-10(agape250 機・Opus 5)🔄 手機不必下滑也能拿到新版 + 🧹 修好本機 build 崩潰 ✅ 已上線(6637de8,v8)★★★★★
  由來:使用者實機回報「在手機無法用手指下滑,來重新整理成為最新版」——他拿不到我們剛修好的版本。
  ① 🔄 為什麼不去「把下拉更新打開」:本站 body/#root 是 `height:100vh; overflow:hidden`
     (整個視窗讓給 3D 棋盤),而瀏覽器的下拉更新只在「文件可捲動且捲到頂」才會觸發 ⇒ 怎麼拉都沒反應。
     要打開它就得讓頁面可捲,那樣手指拖棋盤轉視角會整頁跟著捲 —— 換一個更糟的病。
     ⇒ 改成「不必下滑也拿得到新版」:按鈕列加一顆「🔄 更新」(先 reg.update() 再 reload;
       只 reload 拿到的還是快取那份舊的)、聽 controllerchange 自動 reload 一次(reloaded 旗標防迴圈)、
       開啟時 + 每次從背景切回前景 + 每 30 分鐘主動問一次有沒有新版
       (手機使用者幾乎不關分頁、只切走再切回,沒有這條就永遠不會去問)。
  ② 🧹 `npm run build` 本機原生崩潰(0xC0000409,**沒有任何錯誤訊息**)—— 完整查法寫在
     `scripts/clean-dist.mjs` 檔頭,務必先讀那一段再動這裡。摘要:崩潰的不是 Vite、不是這輪改動
     (git stash 後對原始碼跑一樣崩),是「用 node 刪掉 dist/」這個動作本身 ——
     fs.rmSync 一碰 dist/ 底下任何 entry 就把整個行程打掛,而 Git Bash 的遞迴刪除與
     `rmdir /s /q` 都刪得掉 ⇒ 新增 `prebuild` 腳本走 rmdir,刪完回頭確認真的不見了才讓 build 繼續。
     ⚠⚠ **不要靠「關掉防毒」解決**:0910 當場試過,關掉 PC-cillin 之後**反而更糟**
       (原本只有 dist/ 會崩,變成連剛新建的空目錄都崩)⇒ 正解是開回防毒 + 重開機,
       真的還會咬就給開發資料夾加排除。本站的 rmdir 繞法在哪一種狀態下都有效。
  ③ 🔬 新增 `scripts/check-mobile-ui.mjs`(`npm run check:mobile`)守這輪三條退件:
     橫向看得見「展開/收起」且按得動、轉向後按「重置視角」camera.aspect 要跟上畫布、
     「🔄 更新」鈕在且不會重整迴圈。`window.__anchess` 加了唯讀的 camAspect / canvasAspect
     (只看截圖分不出是相機錯還是畫布錯)。★ 突變驗過它咬得住:把 fitRef 修法退回舊寫法
     ⇒ 量到 cam=0.462 vs canvas=2.164 當場紅。
  測試:npm test(ai 11・version 38・legality 20・fit 19)、npm run check:mobile 11/0、
    lint 前後同樣 6 項(都是既有的)。
  部署:三站逐一 `npx wrangler pages deploy dist --project-name <名> --branch main --commit-dirty=true`,
    指紋三站一致 index-u7t7d5Bv.js,線上抽驗過更新鈕 / controllerchange / v8 都在。
    ⚠ `npm run deploy` 這輪跑不完:wrangler 上傳**成功之後**、行程收尾時被同一個
      原生崩潰打斷,deploy-all.mjs 因此停在第一站 ⇒ 它停掉不代表前面那站沒上去,
      看到「Deployment complete」就是上去了,剩下兩站逐一補推即可。
  git 實況(交接前真的問過 git):status --short 空、HEAD eeb7d28、rev-list @{u}..HEAD = 0。

★★★★★ 2026-09-10(agape250 機・Sonnet 5)🗂 展開/收起手機橫向消失 + 🎥 重置視角轉向後棋盤變扁 ✅ 已上線(aea45f7,隨 v8 一起部署)★★★★★
  由來:使用者拿當天早上的手機截圖體檢三站後,又親自到線上實測回報兩件:「展開與收起會有時
       DISABLE」「橫向重置視角後,棋盤太扁,這角度看不到棋子,無法玩,建議朝上順時鐘轉45度或60度」。
  ① 展開/收起有時 DISABLE(其實是消失,不是真的被鎖):src/index.css 的 `.panel-toggle` 只在
     `@media (max-width:768px)` 顯示,手機轉橫向後寬度常超過 768px(例如 844px),斷點失效,
     鈕直接 display:none —— 不是被鎖住,是根本沒畫出來。改成 `@media (max-width:768px),
     (max-height:500px)`,寬螢幕或矮螢幕(橫向手機)任一成立就算手機版。
  ② 重置視角轉向後變扁:src/App.jsx 的 `<FitCamera>` 裡,`fitRef.current`(「重置視角」鈕綁的
     函式)以前只在 `useEffect(..., [camera, is2D])` 裡賦值 —— 轉向(width/height 變了但
     camera/is2D 沒變)不會讓這個 effect 重跑,於是按鈕永遠綁著轉向**之前**那個閉包,裡面關住
     的是舊的 width/height。按下重置視角時 `fitCamera()` 把 `camera.aspect` 強制寫回那個舊比例,
     跟畫布現在真正的寬高對不上,畫面因此被拉扁。改成每次 render 後都刷新 `fitRef.current`。
  ★ 使用者同時反映「AI 提示後點綠點沒反應,三款都有」——查證過**本站沒有**這個問題:
    `showHint()` 本來就會 `setSelectedPiece(best.from)`(見該行註解「順手選起來」),點提示畫
    的紫色終點走的是與一般走子相同的 `tryMove` 路徑,原本就走得動。這條只有姊妹站 3D-Xiangqi /
    xiangqi-arena 真的有(那兩站的 paintHint 只畫視覺、沒有同步選取狀態),已在那兩個 repo 各自修好。
  🏷 版本簡歷 v6 → v7(src/version.js),文案同步兩則改動。
  測試:npm test(ai 11・version 34・legality 20・fit 19,全綠)。
  🟡 部署:**這輪沒有部署**——`npm run build` 在這台機器(agape250)上會 native crash
    (exit code -1073740791,Windows STATUS_STACK_BUFFER_OVERRUN 類的崩潰)。已用 git stash
    驗過:對**原始未改動**的程式碼跑一樣崩潰 ⇒ 是這台機器既有的 Vite v8 / Rolldown 環境問題,
    不是這輪改動造成的。程式碼已經 commit + push(GitHub 是新的),線上三個 CF Pages 專案
    (3d-chinese-chess / 3dchinesechess / 3dchinese)還是**舊版 v6**,等環境問題排除或
    換一台機器 build 之後記得補跑 `npm run deploy`(見「怎麼跑/測/部署」段)三站都要部署。
  git 實況(交接前真的問過 git):status --short 空、HEAD aea45f7、rev-list @{u}..HEAD = 0
    (程式碼已經在 origin 上,只有**線上網站**還沒更新)。

★★★★★ 2026-09-07 🎯 提示品質大修 ✅ 已上線(fc45626;三個 CF 專案都部署了)★★★★★

  由來:使用者在姊妹站 3D 幻影西洋棋退件——
        「AI 提示為何常叫我吃掉哪一個對手,我吃掉之後又被對手其他棋子吃掉,這樣等於交換被吃」
        ⇒ 做了一輪**全棋類提示體檢**,本站是紅燈(兩條經典病因都在,外加一條自己獨有的)。

  病因(確診,不是猜):
    ① 同分偏好吃子:evaluateBoard 只加總子力 ⇒ 中局九成走法是 0 分平手;
       根層 moves.sort(random) + 「嚴格變好才換人」⇒ 平手裡隨機挑,等價交換被當成建議送出。
    ② 水平線效應:提示深度 3 是奇數層,「我吃→他回吃→我再吃」看起來賺,第 4 步被吃回看不到。
    ③ 本站獨有:提示深度跟著玩家選的難度走(Math.max(difficulty,6))⇒ 選「簡單」時只有 depth 2。

  改動(src/game/ai.js + src/App.jsx):
    - PST 位置表(沿用 3D-Xiangqi 已驗證那組)。⚠ 座標系不同:紅子查 PST[9-y][x]、黑子查 PST[y][x];
      士象王不給位置分。
    - 內層 minimax 抽成模組層 searchAlphaBeta(呼叫端簽名不變,用一個本地箭頭函式包住);
      葉子改走 quiescence(只續走吃子,qdepth 4)。
    - MVV-LVA orderMovesInPlace:不改分數只改快慢 ⇒ 提示 123ms → 57ms。
    - getHintMove():固定 depth 3、零隨機、兩段式(先安靜手,吃子要過①子力關 captureGain>0
      ②分數關 HINT_TRADE_MARGIN=50 半個兵)。App.jsx 的 showHint 改呼叫它。
    - AI 對手仍走 getBestMoveAlphaBeta,棋力不受門檻約束(只是跟著吃到 PST 的紅利)。

  測試:新增 test/ai.mjs 11/0(4 個手工陷阱局面 + 30 個隨機中局用**獨立裁判** refQuiesce 驗
        「沒有比場上最好的一手明顯虧」+ 零隨機 + 耗時 avg 57ms max 130ms);npm run check 10/0。
        npm test 從無到有(以前 package.json 沒有 test script)。

  ⚠⚠ 測試判準的教訓(第一版假紅了三筆,兩筆還是安靜手):
     判準**不可以**是「這一手的淨子力 ≥ 0」——局面本來就在挨打時,每一手都是負的,那不是提示的錯。
     要問的是「**有沒有比場上最好的那一手明顯虧**」。安靜手再放寬一個兵(裁判只看子力,看不出位置補償)。

  🟡 順手發現、**這輪沒有動**的既有問題:
     - npm run lint 有 4 個問題(3 error 1 warning),改動前後數量完全相同 ⇒ 是既有的,不是這輪造成。
     - README.md 在這輪之前一直是 Vite 預設範本(「React + Vite」那份),已重寫。

★ 這是什麼
  React + Three.js 的 3D 中國象棋 PWA(Vite 建置)。單機對 AI,難度 1~10,含開局譜。
  GitHub summer09201017-cloud/3d-chinese-chess(分支 main)。
  ⚠ **同一份 build 部署到三個 Cloudflare Pages 專案**(3d-chinese-chess / 3dchinesechess / 3dchinese),
    三個網址內容相同。改一次要部署三次,否則三站會漂。

★ 現在到哪(2026-09-07)
  ✅ 本輪:💡 提示品質大修(見上面 ★ 段),三站都已部署、build 指紋三站一致。
  ✅ 先前:⛶ 手機放大鈕(0907 別場批次,91e3750)、📡 play-stats 三層打點(88db727)。
  ⏩ 進行中:無。
  🔴 待做第一件:無 🔴。
  📋 待使用者拍板:無。

  git 實況(2026-09-07 交接前真的問過 git,不是憑印象):
    git -C 3d-chinese-chess status --short   → 空
    git -C 3d-chinese-chess log --oneline -1 → fc45626
    git -C 3d-chinese-chess rev-list --count @{u}..HEAD → 0(真的推上去了)

★ 怎麼跑 / 測 / 部署
  npm install
  npm test                      # test/ai.mjs 11 項(提示品質)
  npm run build                 # 產出 dist/
  npm run serve                 # 另一個視窗:靜態伺服 dist/(埠 8799)
  npm run check                 # 真瀏覽器冒煙 10 項(💡 提示)
  部署(⚠ 直傳站,push 不會上線;而且要傳三次):
    npx wrangler pages deploy dist --project-name 3d-chinese-chess  --branch main --commit-dirty=true
    npx wrangler pages deploy dist --project-name 3dchinesechess    --branch main --commit-dirty=true
    npx wrangler pages deploy dist --project-name 3dchinese         --branch main --commit-dirty=true
  線上驗收(比 build 指紋,三站要一致):
    ls dist/assets/index-*.js
    curl -s "https://3d-chinese-chess.pages.dev/?b=$RANDOM" | grep -o 'assets/index-[A-Za-z0-9_-]*\.js'

★ 本機地雷
  - PWA 走 vite-plugin-pwa 的 autoUpdate ⇒ 每次 build 產新 SW,**不必手動 bump 版號**
    (與艦隊其他手寫 sw.js 的站不同,別照抄那套 CACHE_NAME +1 的流程)。
  - getLegalMoves() 是**偽合法**(原始碼註解寫明不檢查自將),終局靠「王被吃掉」判。
    加任何搜尋邏輯都要照這套語意,別假設它會擋自將。
  - 搜尋分數是「紅方為正」的絕對分,不是 negamax;maximizing ⟺ engine.turn === 'w'。
  - 頁面 <title> 是 3d-an(沿用範本專案名),不是打錯字。
  - 姊妹站 3D-Xiangqi / xiangqi-arena 是另外兩個 repo、另外兩個 CF 專案,引擎與題庫都不同。

★ 檔案在哪
  src/App.jsx(接線 + 提示標記)/ src/game/logic.js(GameEngine)/ src/game/ai.js(評估與搜尋)
  test/ai.mjs / scripts/browser-check.mjs / scripts/serve-dist.mjs
  給人讀:README.md(這輪從 Vite 範本重寫)

★ 下一步
  無指定;若要動引擎,先讀 src/game/ai.js 檔頭那三段註解(PST 座標、quiescence、提示兩關)。

★ 一鍵裝齊 skill
  /take-over(pull hfpc-claude-skills → install.bat → sites.json → /sit-down)
