# roadmap — 3D 中國象棋(`3d-chinese-chess`)· 2026-09-09

使用者 0909 拍板:**這一站會變主力**(「我很喜歡這站,以後會常玩,還會超越其他象棋站」)
⇒ 以下是實地看過原始碼之後的優缺點與待做排序,依 **CP 值**(價值 ÷ 工時)排。

---

## 一、優點(它憑什麼當主力)

1. **觀感最好** —— 象牙白棋子 + 綠邊、米白盤 + 深咖啡格線、深板岩藍背景;
   選中/被提示的棋子**整顆變橘**。這一套是使用者親自點名的,0909 已經反向移植到另外兩個象棋站。
2. **難度光譜最寬** —— 1~10 級(對應搜尋深度 2~5),另外三站只有三檔。
3. **開局譜五種**(中炮 / 屏風馬 / 飛象 / 挺卒 / 純搜尋)—— 電腦不會每盤都下一樣的開局。
4. **技術底子最好** —— React + Vite + R3F,`GameEngine` 是純類別(含 Zobrist hash),
   要加功能、要寫測試都比另外兩站的 vanilla + 全域 class 好動。
5. 已有:悔棋、存讀檔、2D/3D 切換、重置視角、可安裝 PWA、手機放大鈕、
   點棋子顯示可走點、統計打點三層、版本號與改版簡歷(0909 補)。

## 二、缺點(照嚴重度排,**規則層的排最前面**)

### 🔴 R1 走得出「讓自己被將」的棋 —— 判定錯,不是缺功能

`GameEngine.getLegalMoves()`(`src/game/logic.js:67`)是**純幾何**的:產生每個棋子的走法,
**沒有過濾「走完之後自己的王會被吃」**。所以現在的流程是:

> 你走了一步送將 → 電腦下一手把你的帥吃掉 → 畫面 alert「紅方帥被吃,黑方獲勝」

象棋規則裡那種走法是**不合法、不准走**,不是「走了就輸」。孩子會莫名輸掉一盤而不知道為什麼。
姊妹站 `xiangqi-arena` 的 v5 就是補這一條(「不能走出讓自己被將的棋」),有現成做法可抄。

### 🔴 R2 沒有「飛將」(兩王照面)

grep 全 repo 沒有任何飛將/照面的判定。缺這條有兩個後果:
① 允許出現**非法局面**(兩王在同一直線且中間無子);② **漏掉一種真的殺法** —— 飛將是常見殺型,
電腦不會用、玩家用了也不算。

### 🟠 R3 終局用 `alert()`

`src/App.jsx:125/130/137/144` 四個終局都是 `alert()`。手機上是系統對話框:醜、會卡住、
而且蓋掉棋盤讓人看不到最後那一手。另外三站都是畫面內的蓋板。

### 🟠 R4 沒有長將(連續追殺)限制

只有「重複走法三次判負」。象棋的長將規則是「同樣追殺將帥最多連續 3 回」,
兩者不同;`3D-Xiangqi` / `xiangqi-arena` 有現成實作。

### 🟡 R5 沒有每日回訪的理由

沒有每日殘局、沒有戰績成就、沒有計時。使用者說「會常玩」——目前**沒有一個機制讓人每天想開**。
另外兩站的每日殘局是它們近 30 天開啟數(21 / 19)遠高於本站(2)的主因。

### 🟡 R6 提示只有「不虧本的一手」,不會算必勝

0907 已修掉「白做工的交換」,但它仍然是**位置分引擎**:不會告訴你「這是 3 手必勝的第一手」,
殘局裡會把子晃來晃去。姊妹站 0908 已改成先窮舉必勝殺法。

### 🟡 R7 沒有聲音

零音效、零配樂。落子沒有回饋。

### ⚪ R8 小事

- 沒有「重做」(只有悔棋)。
- 沒有棋譜面板 / 回放 / 分享連結(`checkers` 有現成的「整局編碼進網址」)。
- `src/App.css` 是**死檔**(沒有任何檔 import 它);樣式一律寫 `src/index.css`。
- 沒有本機雙人(兩個人用同一台手機下)。

---

## 三、待做排序(CP 值 = 價值 ÷ 工時)

「現成」= 艦隊其他站已經寫過並驗過,抄過來即可 ⇒ CP 值自動變高。

| # | 項目 | CP | 工時 | 為什麼值得 / 備註 |
|---|---|---|---|---|
| 1 | **R1 不准走出送將的棋** | ★★★★★ | 0.5 天 | 這是**錯的判定**,不是缺功能。孩子會莫名輸掉。現成(arena v5) |
| 2 | **💡 提示先算「幾手必勝」** | ★★★★★ | 1 天 | 主力站的招牌;使用者 0909 點名要。現成引擎,但**座標系要轉**(見下面警告) |
| 3 | **📅 每日殘局(每天一組 5 題)** | ★★★★★ | 1~1.5 天 | 唯一能讓人**每天想開**的機制;現成(題庫 + 日期→題目 + 進度),同樣要轉座標 |
| 4 | **R3 終局改畫面內蓋板** | ★★★★☆ | 1 小時 | 手機上 alert 很傷;順手把「再來一局 / 下一題」放進蓋板 |
| 5 | **R2 飛將** | ★★★★☆ | 1~2 小時 | 規則完整度 + 多一種殺法;判定很短(同直線、中間無子) |
| 6 | **音效(落子/吃子/將軍/勝負)** | ★★★★☆ | 2 小時 | 現成 skill `sfx-kit`:Web Audio 即時合成,**零音檔**、可離線、可關 |
| 7 | **勝利彩帶** | ★★★★☆ | 1 小時 | 現成 skill `win-confetti`;和第 4 項一起做最省 |
| 8 | **R4 長將限制** | ★★★☆☆ | 0.5 天 | 規則完整度;現成 |
| 9 | **重做(配對現有的悔棋)** | ★★★☆☆ | 1 小時 | 悔棋已有,重做幾乎免費 |
| 10 | **戰績 + 成就**(勝場/最短將死/連勝) | ★★★☆☆ | 0.5 天 | 回訪理由第二名;localStorage 就夠 |
| 11 | **棋譜分享連結**(整局編碼進網址) | ★★★☆☆ | 0.5 天 | 現成(`checkers` 已有);老師可以把一個局面丟給全班 |
| 12 | **計時模式**(每步限時) | ★★★☆☆ | 0.5 天 | 快棋很受歡迎;現成(chess5 有) |
| 13 | **本機雙人**(同一台手機兩個人下) | ★★★☆☆ | 2~3 小時 | 主日學兩人一機最常見的玩法 |
| 14 | **棋譜面板 + 回放** | ★★☆☆☆ | 1 天 | 教學用;要 UI 空間 |
| 15 | ✅ **殘局編輯器**(2026-09-13 完工上線 v15,使用者點名「自訂殘局」) | ★★☆☆☆ | 1 天 | 老師出題用;也是寫測試局面的工具。含存/載/刪(localStorage)、`?fen=` 分享連結、開始前規則檢查;順手把第 11 項的「局面分享連結」做掉一半(整局棋譜還沒) |
| 16 | **語音播報**(烤 mp3 神經人聲) | ★★☆☆☆ | 1~2 天 | 有趣但貴;鐵則是**不可以用機器合成聲**,要預烤 |
| 17 | **線上對戰(PeerJS)** | ★☆☆☆☆ | 2~3 天 | 最貴、最容易壞(NAT/斷線);建議最後再說 |

### 建議的第一批(約 2.5~3 天,做完就是質變)

**第 1、2、3、4、5 項** —— 規則修對(1、5)+ 招牌功能(2、3)+ 終局體驗(4)。
做完之後這一站在「規則正確 + 提示強度 + 每天有事做」三件上會**同時超過**另外兩個象棋站,
而觀感本來就是它最強。

### ⚠⚠ 做第 2、3 項之前一定要先讀這一條

**本站的座標系和姊妹站相反。**

| | 姊妹站(`3D-Xiangqi` / `xiangqi-arena`) | 本站 |
|---|---|---|
| 陣列 | `board[row][col]` | `board[y][x]` |
| 紅方在 | `row 0~4`(下),紅兵往 **row 增加** 走 | `y 6~9`,紅兵往 **y 減少** 走 |
| 棋子表示 | `{ color, type }` 物件 | 單字元(大寫紅 `KABNRCP` / 小寫黑) |

`README.md` 已經記著這個座標差**咬過一次**(0907 移植 PST 位置表時)。
所以移植提示引擎與殘局題庫時:

1. **先寫座標轉換 + 一支測試**:把姊妹站的 18 題殘局轉進來之後,
   用本站的引擎重新證明「還是 N 手必勝」(不是只看棋子擺對位置)。
   ★ 只比對座標會過關,但**規則不同就不是同一題**了 —— 一定要用「解得出來」當驗收條件。
2. 提示的必勝殺法搜尋(`findForcedMate`)依賴「合法走法」的定義 ⇒
   **必須先做第 1 項(不准送將)**,否則搜尋會把送將的棋當成合法應手,算出假的殺法。
   這也是為什麼第 1 項排在第 2 項前面,不只是因為它便宜。

---

## 四、0909 使用者三個提問的結論(實機量過)

### ⓐ 「手機全螢幕時棋盤能更大嗎」→ **前提要更正:它不是太小,是太大、兩側被切掉**

實機 390×844 直向截圖(選單展開與收起兩種都拍了):**最左與最右兩路被裁在畫面外**
(邊緣的「砲」「馬」只剩半顆、底線的「俥」也被切)。所以問題不是不夠大,是**裝不下**。

病根和 `3D-Xiangqi` 0909 修掉的是同一個:相機寫死在 `[0, 8, 8]`、`fov` 只分手機/桌機
(`isMobile ? 55 : 45`),**沒有照畫面長寬比算距離**。直向 aspect 0.46 時水平視野不夠,棋盤就溢出。

⇒ 要做的是 **fitCamera(照 fov 與 aspect 反推距離,寬高各算一次取大的)**,不是「放更大」。
做完之後:棋盤**完整、吃滿螢幕寬**(390 of 390),而且轉向/進出全螢幕都會重算。
⚠ 誠實講清楚:修好之後棋盤看起來會比現在**略小**一點 —— 因為現在是「被裁掉」才顯得大。
   但九路全看得到、邊路的車馬點得到;棋盤缺了兩路是不能玩的。

★ 直向的上限就是螢幕寬:9 路寬 × 10 路高的盤,寬是限制邊 ⇒ 390 寬就是最大,
  高度會用掉約 51%,上下必然留白(那是幾何,不是沒調好)。橫向則是高度為限制邊。

**CP:★★★★★ / 工時 1~2 小時**(現成:抄 `3D-Xiangqi` 的 `fitCamera`)。排在待做第 1 批。

### ⓑ 「黑棋子的字要不要轉 180°」→ **單人對 AI 不要轉;等做了雙人再依模式自動轉**

現況:`src/components/Piece.jsx` 只有一個 `rotation={[-Math.PI / 2, 0, 0]}`,
紅黑都**朝玩家扶正** —— 這和艦隊的既有決定一致:`3D-Xiangqi` v4 就是因為使用者退件
「棋子的字都歪了」才把**所有**字扶正朝玩家的。

- **單人對 AI(本站主要模式)**:你坐紅方這側看盤,黑字轉 180° 之後你要讀顛倒的字。
  對正在認國字的孩子特別吃力 ⇒ **不要轉**。
- **兩個人面對面用同一台手機**:這時轉 180° 才是對的(實體棋盤就是這樣)。
  ⇒ 正解是**跟著模式自動切**,等做了「本機雙人」(待做 #13)再一起做,不要現在加。
- 「讓玩家自由決定角度」**不建議**做成滑桿/任意角度:沒有人想要 37°,
  而每多一個自由度就多一件要決定的事。真要給選項就給**兩檔**:「朝我 / 朝對面」,預設「朝我」,
  而且掛在雙人模式底下,不要放進主選單。

### ⓒ 「全螢幕時選單能收起/展開」→ **好,而且比我 0909 在對局場做的做法更好**

本站的 `panelOpen`(▲ 收起 / ▼ 展開)是對的設計:**收起時棋盤拿到整個畫面,要用再開**。

而我 0909 在 `xiangqi-arena` 做的是「常駐半透明浮層」——量出來佔 98px = 視窗高的 12%,
永遠都在。**收起式勝過常駐式**:12% 不多,但那 12% 是白給的。

⇒ 待做:把「可收起」補進 `xiangqi-arena` / `3D-Xiangqi` / `chess5` 的全螢幕 HUD
(預設收起、記住上次狀態、一顆常駐的小把手負責展開),並寫進 skill
`board-game-designer`(和「哪一顆要染主體」那幾節放一起)。
**CP:★★★★☆ / 工時 每站 1 小時 + skill 20 分**。
